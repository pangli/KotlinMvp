package com.zorro.kotlin.samples.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.zorro.kotlin.samples.db.entity.LocalFile;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "LOCAL_FILE".
*/
public class LocalFileDao extends AbstractDao<LocalFile, Long> {

    public static final String TABLENAME = "LOCAL_FILE";

    /**
     * Properties of entity LocalFile.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property FileUrl = new Property(1, String.class, "fileUrl", false, "FILE_URL");
        public final static Property LocalFileUrl = new Property(2, String.class, "localFileUrl", false, "LOCAL_FILE_URL");
    }


    public LocalFileDao(DaoConfig config) {
        super(config);
    }
    
    public LocalFileDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"LOCAL_FILE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"FILE_URL\" TEXT UNIQUE ," + // 1: fileUrl
                "\"LOCAL_FILE_URL\" TEXT);"); // 2: localFileUrl
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"LOCAL_FILE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, LocalFile entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String fileUrl = entity.getFileUrl();
        if (fileUrl != null) {
            stmt.bindString(2, fileUrl);
        }
 
        String localFileUrl = entity.getLocalFileUrl();
        if (localFileUrl != null) {
            stmt.bindString(3, localFileUrl);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, LocalFile entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String fileUrl = entity.getFileUrl();
        if (fileUrl != null) {
            stmt.bindString(2, fileUrl);
        }
 
        String localFileUrl = entity.getLocalFileUrl();
        if (localFileUrl != null) {
            stmt.bindString(3, localFileUrl);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public LocalFile readEntity(Cursor cursor, int offset) {
        LocalFile entity = new LocalFile( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // fileUrl
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // localFileUrl
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, LocalFile entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setFileUrl(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setLocalFileUrl(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(LocalFile entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(LocalFile entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(LocalFile entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
